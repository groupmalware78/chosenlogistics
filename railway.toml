[build]
buildCommand = "npm ci --prefix frontend && npm run build --prefix frontend && npm ci --prefix backend && npx --prefix backend prisma generate"

[deploy]
startCommand = "cd backend && npx prisma db push && node src/index.js"
healthcheckPath = "/api/auth/login"
healthcheckTimeout = 30
